struct point
{
    u64 x;
    u64 y;
}

struct rect
{
    point pos;
    point size;
}

u64 main()
{
    rect r;

    r.pos.x = 3;
    r.pos.y = 7;

    r.size.x = 5;
    r.size.y = 10;

    return r.size.x + r.pos.y;
}
