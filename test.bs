ulong inc(ulong a)
{
    ulong ret;

    ret = a + 1;

    return ret;
}

ulong doubleinc(ulong a)
{
    ulong ret;

    ret = inc(a);
    ret = inc(ret);

    return ret;
}

ulong main()
{
    ulong x;
    x = 3;

    ulong y;
    y = doubleinc(x);

    return y;
}
