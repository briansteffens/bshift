import heap;

u64 main()
{
    auto t0 = heap::init();

    u64* a = heap::allocate(sizeof(u64));
    *a = 3;

    auto t1 = heap::free(a);

    u64* b = heap::allocate(sizeof(u64));

    return *b;
}
