runners:
 - basm
 - nasm

cases:
 - name: call a struct method

   status: 10


templates:
 - test.bs: |

    struct point
    {
        u64 x;
        u64 y;
    }

    u64 point::sum()
    {
        return this.x + this.y;
    }

    u64 main()
    {
        point p;

        p.x = 3;
        p.y = 7;

        return p.sum();
    }
