import cstring;

u64 test_0(u64 t0)
{
    u8[16] str;
    u64 t1 = cstring::from_u64(0, &str);

    return str[0] == 48 && str[1] == 0;
}

u64 test_7(u64 t0)
{
    u8[16] str;
    u64 t1 = cstring::from_u64(7, &str);

    return str[0] == 55 && str[1] == 0;
}

u64 test_137(u64 t0)
{
    u8[16] str;
    u64 t1 = cstring::from_u64(137, &str);

    if (str[0] != 49)
    {
        return 0;
    }

    if (str[1] != 51)
    {
        return 0;
    }

    if (str[2] != 55)
    {
        return 0;
    }

    return str[3] == 0;
}

u64 main()
{
    return test_{{ test }}(0);
}

-----

{
    "cases": [
        {
            "name": "0 = '0'",
            "test": "0",
            "expect_status": "1"
        },
        {
            "name": "7 = '7'",
            "test": "7",
            "expect_status": "1"
        },
        {
            "name": "137 = '137'",
            "test": "137",
            "expect_status": "1"
        }
    ]
}
