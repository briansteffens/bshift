runners:
 - basm
 - nasm

cases:
 - name: import normal function without qualification

   status: 5


templates:
 - test.bs: |

    import length from cstring;

    u64 main()
    {
        return length("hello");
    }
